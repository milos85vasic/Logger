apply from: "../build.gradle"
apply from: "../commons.gradle"
apply from: "../compile.gradle"
apply from: "../deployment.gradle"

final alpha = 0
final beta = 0
final versionNumber = 1
final secondaryVersion = 2
final tertiaryVersion = 0
final versionPackage = "net.milosvasic.logger"

setupProject(
        alpha,
        beta,
        versionNumber,
        secondaryVersion,
        tertiaryVersion,
        versionPackage
)

setupDeployment(project.version, versionPackage)
